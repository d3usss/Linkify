version: '3.8'

services:
  mysql:
    image: mysql
    container_name: mysql_db_linkify
    env_file:
      - .env.service.local
    ports:
      - '3306:3306'
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
